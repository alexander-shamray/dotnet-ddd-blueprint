apiVersion: v2
name: catalog
description: >-
  Catalog (§4.1) — its API, its migrator hook, and the platform's one gRPC
  server (§9.7).
type: application
version: 0.1.0
dependencies:
  - name: commerce-common
    version: 0.1.0
    repository: file://../common
