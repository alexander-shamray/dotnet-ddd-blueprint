name: Deploy

# §15.5's canary, and §15.1's production leg of the pipeline.
#
# THIS WORKFLOW HAS NEVER REACHED A CLUSTER, and that is stated here rather
# than discovered from a failed run. There is no dev, staging or production
# environment for this repository, no kubeconfig and no registry; PR-23 shipped
# charts that nothing installs and PR-24 alert rules that no Prometheus loads,
# and this is the third artefact of that kind. What is asserted about it is
# asserted by `deploy/canary/canary.py`, whose suite runs on every change to
# this tree, and by the `check` job below, which reads this file.
#
# `workflow_dispatch` ONLY, and the reason is `main` staying green. A canary on
# `push` would fail on every merge for want of a cluster, which trains everyone
# to ignore a red pipeline -- the exact failure §15.1 spends a paragraph on
# about gates nobody has defined. When an environment exists, the trigger to
# add is `workflow_run` on a green CI over `main`, plus §15.1's manual approval
# gate between staging and production; the steps below do not change.
on:
  workflow_dispatch:
    inputs:
      workload:
        description: 'Which deployable to roll (a key in deploy/canary/canary.json)'
        required: true
        type: choice
        options: [catalog-api, ordering-api, gateway, web-bff]
      tag:
        description: 'Image tag to promote. §15.3 refuses an empty one rather than rolling :latest.'
        required: true
        type: string
  # The gate runs on its own inputs, so a change to the plan, the script or
  # this file is checked without a deploy. `canary.py` declares SOURCE_INPUTS
  # beside the reads and asserts BOTH lists below cover every entry -- the Helm
  # tree's lesson, adopted rather than re-learned.
  pull_request:
    paths:
      - 'deploy/canary/**'
      - '.github/workflows/deploy.yml'
      - 'deploy/helm/**'
      - 'deploy/observability/**'
      - 'src/**'
  push:
    branches: [main]
    paths:
      - 'deploy/canary/**'
      - '.github/workflows/deploy.yml'
      - 'deploy/helm/**'
      - 'deploy/observability/**'
      - 'src/**'

permissions:
  contents: read

concurrency:
  # NOT cancel-in-progress. Cancelling a rollout mid-ladder leaves a canary
  # Deployment serving traffic with nothing left to promote or retire it, which
  # is the one state this whole file exists to avoid. A second run queues.
  #
  # KEYED BY WORKLOAD, NOT BY REF, and the ref is what it used to be. Two
  # manual dispatches of the same workload from different branches are two runs
  # against ONE stable release, one HPA floor and one `<workload>-canary` — and
  # under a ref-scoped key they proceed in parallel, each restoring a floor the
  # other moved and each uninstalling the other's canary. The thing that must
  # serialise is the cluster object, and the branch a dispatch was launched
  # from says nothing about which one that is. `workflow_dispatch` is the only
  # trigger that reaches this job, so `inputs.workload` is always present.
  group: deploy-${{ inputs.workload }}
  cancel-in-progress: false

jobs:
  # Stdlib Python over text, so it needs no cluster, no restore and no SDK --
  # the licence gate's terms, which is why it can gate the deploy below.
  check:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Test the canary's arithmetic
        working-directory: deploy/canary
        run: python -m unittest

      - name: The rollout plan agrees with the charts and the alerts
        run: python deploy/canary/canary.py check

  rollout:
    needs: check
    if: ${{ github.event_name == 'workflow_dispatch' }}
    runs-on: ubuntu-latest
    # ARITHMETIC, not a round number, and 60 was less than the rollout's own
    # declared maximum. §15.5's ladder dwells ten minutes on each of three
    # rungs (30), and each of the three canary upgrades plus the promotion may
    # take the 20 minutes §15.1's callout requires for a migration hook (80) —
    # 110 in total, before the cleanup step. A 60-minute cap cancelled a valid
    # slow migration mid-rollout without any per-command timeout being hit,
    # which is the one way to leave a canary serving with no step left to
    # remove it.
    timeout-minutes: 150
    environment: production
    env:
      NAMESPACE: commerce
      # A GitHub Environment scopes secrets and variables; it does not put them
      # in the process. The reader takes the standard variable, so it is bound
      # here explicitly rather than assumed to arrive.
      PROMETHEUS_URL: ${{ vars.PROMETHEUS_URL }}
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      # THE STEP THAT AUTHENTICATES TO A CLUSTER IS MISSING, and naming it is
      # better than a comment implying it is handled. `ubuntu-latest` is a
      # fresh hosted runner with no kubeconfig, and a GitHub Environment scopes
      # secrets without materialising one — so the first `kubectl` below has no
      # context and this job cannot be dispatched as it stands. What it is
      # owed, in this order: a checkout of the cluster credential (an OIDC
      # login for the provider, or `azure/k8s-set-context`-style step), and
      # `helm` on the runner, which `ubuntu-latest` does ship but no line here
      # pins.
      #
      # It is not written speculatively because there is no cluster to write it
      # against, and a login step for a provider nobody has chosen would be a
      # fiction with credentials in it. `PROMETHEUS_URL` IS bound, in the job's
      # `env` above, because that one needs no provider to be correct.
      #
      # Everything below is here because the decision it encodes is the
      # deliverable, and burying it in a runbook would make it prose.
      # BEFORE ANY HELM COMMAND SEES IT. `--set-string image.tag="$TAG"` is not
      # one assignment: Helm parses the value with `strvals`, where a comma
      # separates assignments — so a dispatch of
      # `deadbeef,image.registry=attacker.example` sets a valid `image.tag`
      # AND overrides the registry, for the canary and the promotion alike.
      # The chart's own validation passes, because by then the tag really is
      # `deadbeef` and the injected key rode in beside it.
      #
      # The rule is `commerce.tag`'s, so the two cannot disagree, and it is
      # tested — a regex inline here would be the one part of this file nothing
      # could exercise.
      #
      # `--workload` because the budget is not the same for every chart: a
      # workload with a migrator derives `<workload>-migrate-<tag>`, which
      # Kubernetes stamps onto every pod as a label and refuses past 63. The
      # chart already checks it — at RENDER time, which on this path is after
      # the stable track has been scaled to nineteen.
      - name: The tag has to be a tag
        run: python deploy/canary/canary.py validate-tag --value "$TAG" --workload "$WORKLOAD"
        env:
          TAG: ${{ inputs.tag }}
          WORKLOAD: ${{ inputs.workload }}

      - name: The first rung has a price, and it is paid before anything rolls
        run: |
          set -euo pipefail
          NEEDED=$(python deploy/canary/canary.py required --step 0)
          echo "The first step needs $NEEDED stable replicas (ADR-022)."
          # §15.5's 5% is one pod in twenty, so a three-replica service cannot
          # express it: one canary already serves 25%. Scaling up is what makes
          # the requested weight the served weight, and it is deliberate rather
          # than implicit -- `canary.py plan` refuses instead of rounding.
          #
          # §15.3's autoscaling.maxReplicas is 20 on the three service charts,
          # exactly this 19 plus one canary — chosen independently, and worth
          # knowing. It is NOT platform-wide: the gateway's is 30, so there the
          # 19 is what the weight costs rather than all the chart allows.
          #
          # **THE HPA'S FLOOR MOVES FIRST, and a `kubectl scale` alone would
          # not have held.** These charts enable an autoscaler on the stable
          # track with `minReplicas: 3`, and _deployment.tpl says in its own
          # comment that `replicas` is a managed field — "the HPA writes it
          # back". So a scale to 19 decays toward 3 across the ten-minute
          # dwells, and `plan` is then computing 5% against a replica count
          # the cluster no longer has. On a busy service the HPA may already
          # be near 19 and the scale is a no-op, which hides the hole rather
          # than closing it.
          #
          # Raising the floor is what makes the number stick, and restoring it
          # is owed FROM THE MOMENT IT IS RAISED. The floor is recorded in
          # GITHUB_ENV before the patch, and the `if: always()` step at the end
          # of this job is what puts it back — see the argument there.
          # `--ignore-not-found`, NOT `2>/dev/null || echo ""`. That form
          # turned every failure into "there is no HPA" — an expired
          # credential, a network blip, an API server saying 403 — and the
          # rollout would then scale the Deployment without raising a floor
          # that does exist, so the autoscaler pulls the stable count back down
          # and every weight the ladder computes is against a number the
          # cluster does not have. Only a genuinely absent HPA may yield an
          # empty string here; anything else has to stop the rollout.
          FLOOR=$(kubectl get hpa "$WORKLOAD" -n "$NAMESPACE" \
            --ignore-not-found -o jsonpath='{.spec.minReplicas}')
          echo "HPA_FLOOR=$FLOOR" >> "$GITHUB_ENV"

          # THE TARGET IS A MAXIMUM, because this step may only ever scale UP.
          # `$NEEDED` is what the first rung's weight requires, not what the
          # service is currently running: a workload the autoscaler has taken
          # to 20 under load would have been forced DOWN to 19, and an HPA
          # floor already above 19 would have been LOWERED to it. Reducing
          # production capacity to start a canary is the opposite of what a
          # canary is for, and it happens at the busiest moment, since a high
          # replica count is what load looks like.
          #
          # Taking the maximum keeps the weight honest too — more stable pods
          # only make the canary's share smaller than the rung asked for, and
          # `plan` treats the requested weight as a ceiling.
          # ONE read of the replica count, used for both the maximum above and
          # the value the cleanup restores. Two reads of one number is how a
          # message ends up naming a figure the check beside it would reject,
          # which this branch has already paid for once.
          CURRENT=$(kubectl get deployment "$WORKLOAD" -n "$NAMESPACE" \
            -o jsonpath='{.spec.replicas}')
          echo "STABLE_REPLICAS_BEFORE=$CURRENT" >> "$GITHUB_ENV"

          for candidate in "$CURRENT" "$FLOOR"; do
            if [ -n "$candidate" ] && [ "$candidate" -gt "$NEEDED" ]; then
              NEEDED="$candidate"
            fi
          done
          echo "Scaling the stable track to $NEEDED (never below what it runs now)."

          # The Deployment's own replica count, recorded whether or not there
          # is an HPA — and it is the ONLY thing to restore when there is not.
          # `autoscaling.enabled: false` is a supported chart configuration
          # (§15.3), and under it the cleanup below had nothing to put back
          # while this step had still scaled production to 19. A rollback then
          # left the service permanently over-scaled, which is the same defect
          # as leaving minReplicas at 19 and is invisible for longer.
          if [ -n "$FLOOR" ]; then
            kubectl patch hpa "$WORKLOAD" -n "$NAMESPACE" \
              --type=merge -p "{\"spec\":{\"minReplicas\":$NEEDED}}"
          fi

          kubectl scale deployment "$WORKLOAD" --replicas="$NEEDED" -n "$NAMESPACE"
          # BOUNDED, because an unbounded wait here does not fail — it consumes
          # the job. A scale-up that cannot schedule 19 ready pods would block
          # until the 150-minute cap, and a job cancelled on its timeout is not
          # the same as a step that failed: the `if: always()` cleanup is not
          # reliably reached, so the raised HPA floor stays raised with nothing
          # left to run. A bounded failure reaches the cleanup; an unbounded
          # one is how the floor survives the rollout.
          kubectl rollout status deployment "$WORKLOAD" -n "$NAMESPACE" --timeout=10m
        env:
          WORKLOAD: ${{ inputs.workload }}

      - name: Climb the ladder
        run: |
          set -euo pipefail

          STEPS=$(python deploy/canary/canary.py steps)
          STABLE=$(kubectl get deployment "$WORKLOAD" -n "$NAMESPACE" -o jsonpath='{.spec.replicas}')

          # `deploy/helm/*/charts/` is gitignored, and every service chart
          # depends on `file://../common`. Without this, the install below is
          # `found in Chart.yaml, but missing in charts/ directory:
          # commerce-common` and the canary never reaches a pod. `smoke.sh`
          # re-resolves on every run and the README's first setup command is
          # this one; the workflow was the copy that did not.
          CHART_DIR="deploy/helm/$(python deploy/canary/canary.py chart --workload "$WORKLOAD")"
          helm dependency update "$CHART_DIR"

          # ONCE, before the ladder, and used by BOTH the canary installs and
          # the promotion. `helm get values` returns the release's own
          # overrides — not the coalesced set — so applying it over the new
          # chart picks up new defaults while keeping what the environment
          # chose. Nothing in this loop touches the stable release, so one
          # fetch is the same answer every rung.
          helm get values "$WORKLOAD" -n "$NAMESPACE" -o yaml > stable-values.yaml

          for STEP in $(seq 0 $((STEPS - 1))); do
            # `plan` prints CANARY_* assignments rather than JSON so this stays
            # a shell script instead of five inline `python -c` invocations,
            # each of them a quoting bug in the one file nothing here can test.
            # It EXITS NON-ZERO when the requested weight is not expressible at
            # the current replica count, and `set -e` is what makes that a
            # stopped rollout rather than a stale plan reused.
            eval "$(python deploy/canary/canary.py plan \
              --workload "$WORKLOAD" --stable "$STABLE" --step "$STEP")"

            # THE LAST RUNG IS HANDLED BEFORE THE CANARY IS TOUCHED, because
            # 100% is not a weight — `plan` says so in its own docstring and
            # returns `final`, with `stableReplicas: 0`, rather than a share.
            #
            # Run in rung order this branch sat AFTER the upgrade below, and
            # every part of that was wrong: the canary was already at this
            # replica count from the 50% step, so the upgrade was a no-op that
            # re-ran §7.4's migration hook for nothing; and the step announced
            # "100.0% served" while the stable track was still up, so the
            # cluster was at 50% and the log said otherwise. Promotion is a
            # different operation from a rung, and it belongs before the code
            # that performs one.
            if [ "$CANARY_FINAL" = "True" ]; then
              echo "The canary is the release; promoting the tag onto the stable release."
              # THE CHART ARGUMENT IS REQUIRED. `helm upgrade` is
              # `[RELEASE] [CHART]`, and `--reuse-values` reuses the values,
              # not the chart — without it this is
              # `Error: "helm upgrade" requires 2 arguments`, at cutover, with
              # the canary still serving.
              #
              # `--atomic` because this is the ONE command in the file that
              # mutates the stable release. Without it a failed hook, rollout
              # or wait leaves production partially upgraded, and uninstalling
              # the canary afterwards cannot put the previous revision back —
              # the rollback this whole mechanism advertises would be gone at
              # precisely the moment it is needed. `--atomic` rolls the stable
              # release back itself before the cleanup step runs. It is
              # deliberately NOT on the canary installs: there a failure is
              # handled by removing the canary, and rolling it back to an
              # earlier canary revision is not a state anybody wants.
              # `-f stable-values.yaml`, NOT `--reuse-values`. The latter
              # carries forward the values coalesced from the PREVIOUS chart
              # and ignores defaults the new one introduces — so the canary,
              # installed from the new chart plus the stable release's own
              # overrides, and the promotion, installed from the old coalesced
              # set, could render different configuration at cutover, or the
              # promotion could fail on a value the new chart added. Both ends
              # now apply the same overrides over the same chart, which is the
              # only way the thing promoted is the thing that was measured.
              helm upgrade "$WORKLOAD" "deploy/helm/$CANARY_CHART" \
                -n "$NAMESPACE" \
                -f stable-values.yaml --set-string image.tag="$TAG" \
                --atomic --wait --timeout 20m
              helm uninstall "$WORKLOAD-canary" -n "$NAMESPACE"
              break
            fi

            echo "step $STEP: $CANARY_CANARY_REPLICAS canary pod(s), ${CANARY_ACHIEVED}% served"

            # The canary is a SECOND release of the same chart, differing only
            # in `canary.enabled`, its replica count and the image tag. The
            # stable release is not touched anywhere in this loop, which is
            # what makes the rollback below cost the canary's own pods and
            # nothing else (ADR-022).
            #
            # **IT INHERITS THE STABLE RELEASE'S VALUES, and `--install` is
            # exactly why it has to.** A new release inherits nothing: without
            # `-f`, every environment value falls through to the chart's own
            # default, so the canary would come up against
            # `identity.example.com`, the default OTLP endpoint and the default
            # database — a pod that is not a canary of anything. The gateway is
            # the one where this fails LOUDLY rather than silently, because
            # `edge-config.yaml` refuses an empty `ingress.trustedNetworks`;
            # the other three would have installed and been wrong. Measured:
            # `helm template` with the flags below and no `-f` fails at
            # edge-config.yaml:194.
            #
            # autoscaling off on the canary because an HPA would move the
            # weight underneath the analysis: the served share is a replica
            # ratio, so a canary that scales itself is a canary whose blast
            # radius nobody chose.
            # `--timeout 20m` because §15.1's callout, deploy/helm/README.md
            # and docs/runbooks/migration-failure.md all state the same rule:
            # Helm blocks on a hook whatever `--wait` says, its default is five
            # minutes, and MigrationJobFailed needs sixteen — so 20m > 16m is
            # the constraint. ADR-022 makes the CANARY release the one that
            # runs §7.4's hook, which is precisely why this command is the one
            # that must not inherit the default.
            #
            # `--set autoscaling.enabled=false` is NOT belt and braces: see
            # _autoscaling.tpl. The values inherited from the stable release
            # have the HPA on, and `_deployment.tpl` omits `replicas` whenever
            # it is — so without this flag the canary Deployment carries no
            # replica count and the API server defaults it to one, making
            # every rung of the ladder a single pod.
            helm upgrade --install "$WORKLOAD-canary" "deploy/helm/$CANARY_CHART" \
              -n "$NAMESPACE" \
              -f stable-values.yaml \
              --set canary.enabled=true \
              --set replicaCount="$CANARY_CANARY_REPLICAS" \
              --set autoscaling.enabled=false \
              --set-string image.tag="$TAG" \
              --wait --timeout 20m

            # The window equals the dwell: a rate over ten minutes, read at the
            # end of ten minutes, IS §15.5's "watch for ten minutes".
            sleep "$((CANARY_DWELL_MINUTES * 60))"
            python deploy/canary/read_prometheus.py \
              --service "$CANARY_SERVICE_NAME" \
              --window "${CANARY_DWELL_MINUTES}m" \
              --out readings.json

            if ! python deploy/canary/canary.py analyse --readings readings.json; then
              echo "::error::Rolling back at step $STEP."
              helm uninstall "$WORKLOAD-canary" -n "$NAMESPACE"
              exit 1
            fi
          done
        env:
          WORKLOAD: ${{ inputs.workload }}
          TAG: ${{ inputs.tag }}

      # ONE mechanism, at the end, for every path — and that is the third
      # attempt at this.
      #
      # Calling a restore at each of the two outcomes covered promote and
      # rollback and missed `set -e`. Moving it onto a `trap EXIT` covered
      # everything the climb step could do and missed the step BEFORE it: the
      # scale-up is where the floor is raised, and a `kubectl` that fails there
      # skips the climb step entirely, because Actions defaults every step to
      # `if: success()`. Each fix was correct about the case it named and
      # scoped to the wrong unit.
      #
      # `if: always()` is scoped to the JOB, which is the unit every one of
      # these is undone for. It runs after a promote, after a rollback, after a
      # failure in any step, and after a cancellation.
      #
      # THREE things are undone here, and the canary is the one that was
      # missing. A failure between the install and either exit — a Prometheus
      # that cannot be reached, a Helm timeout, the analyse step erroring
      # rather than returning a verdict — bypasses both explicit
      # `helm uninstall` calls under `set -e`, and a step that restores only
      # the autoscaler leaves a canary of an unproven build serving a share of
      # production indefinitely. That is a worse outcome than the over-scaling
      # this step was written for.
      - name: Undo everything the rollout raised
        if: always()
        run: |
          failed=0

          # The canary first: it is the one serving traffic.
          #
          # `--ignore-not-found` on the uninstall, NOT a `helm status` guard.
          # The guard was the HPA's `2>/dev/null` fail-open in another costume:
          # `helm status` exits non-zero for an absent release AND for an
          # expired credential, a network failure or an API server saying no,
          # so every one of those skipped the canary entirely — silently, in
          # the step whose whole job is to make sure it is gone. The flag makes
          # absence the only ignored outcome and leaves the rest visible.
          echo "Removing the canary release if the rollout left one."
          helm uninstall "$WORKLOAD-canary" -n "$NAMESPACE" --ignore-not-found || failed=1

          if [ -n "${HPA_FLOOR:-}" ]; then
            kubectl patch hpa "$WORKLOAD" -n "$NAMESPACE" \
              --type=merge -p "{\"spec\":{\"minReplicas\":$HPA_FLOOR}}" || failed=1
          elif [ -n "${STABLE_REPLICAS_BEFORE:-}" ]; then
            # No HPA to own the count, so the scale-up is this step's to undo.
            kubectl scale deployment "$WORKLOAD" \
              --replicas="$STABLE_REPLICAS_BEFORE" -n "$NAMESPACE" || failed=1
          fi

          # NOT `|| true` swallowing the lot. A cleanup that fails is a cluster
          # left in the state this step exists to prevent, and reporting
          # success would hide exactly that — the failure the `|| true` here
          # used to cause. Each command is allowed to fail without skipping the
          # ones after it, and then the step fails.
          exit "$failed"
        env:
          WORKLOAD: ${{ inputs.workload }}
