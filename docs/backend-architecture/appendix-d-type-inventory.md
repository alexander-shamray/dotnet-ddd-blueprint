# Appendix D — Type inventory

Code samples in this document are excerpts, not compilable units. This appendix
is the index of the types they reference, so a reader can tell at a glance
whether a name is defined somewhere in the document, deliberately elided, or a
framework type.

It exists because of a specific failure mode: a sample that calls
`ProjectionInvoker.InvokeAsync(...)` reads as complete, and nothing catches that
no such method was ever defined. Grepping this table against the samples is a
repeatable check; noticing by eye is not.

## D.1 Application ports — defined here

| Type | Section | Purpose |
|---|---|---|
| `ICommand<T>`, `IQuery<T>` | [§6.2](06-cqrs.md) | Request marker interfaces |
| `ICommandHandler<,>`, `IQueryHandler<,>` | §6.2 | Request handlers |
| `IPipelineBehavior<,>`, `NextDelegate<T>` | §6.2 | Cross-cutting pipeline |
| `IDispatcher` | §6.2 | Entry point for commands and queries |
| `IUnitOfWork` | §6.3 | Transaction boundary; EF sealed in Infrastructure |
| `Error`, `ErrorType` | [§10.5](10-api-gateway.md) | `Code`, `Description`, `Type`. `Code` is a metric dimension ([§9.8](09-messaging.md)), so it is a closed set by construction |
| `OrderErrors` | §10.5 | Ordering's catalogue — the only place an `Error` is constructed |
| `IIdempotencyStore`, `IdempotencyEntry` | [§8.5](08-caching-redis.md) | Idempotency-key claim, Redis-backed. `TryClaimAsync` returns a **claim token** rather than a `bool`, and `CompleteAsync` and `ReleaseAsync` take it: a key names the work and only a token names the attempt, so without one neither write could tell this attempt's claim from a successor's. `CompleteAsync` also takes a `string` payload, and what it is handed is a **serialised success value** — never a serialised `Result` — the type D.5 specifies serialises in one of its four states and deserialises in none |
| `IIdempotentCommand` | §8.5 | Opts a command into `IdempotencyBehavior`. Two members, not one: `CommandId`, and a `static abstract OperationName` the compiler forces every implementor to supply — the key's operation segment is declared so that a rename cannot silently change a live key, and must be **unique within a service**, which a gate asserts |
| `IDomainEventCollector` | [§7.5](07-persistence.md) | Reads the change tracker without exposing it |
| `IDomainEventDispatcher` | §7.5 | Stages outbox rows; runs no handlers |
| `IProjectionRegistry` | §7.5 | Which event types have projection handlers |
| `IProductPriceReader` | §6.4 | Prices from the **local** projection — never a remote call |
| `ICurrentUser` | [§11.4](11-identity-authorization.md) | The caller, for resource-level checks, and the **only** source of a subject identifier on a principal-bearing command or query — every HTTP one (§11.4's subject rule) — and of the subject segment in §8.5's idempotency key, which is the same rule applied to something that is not a field. `Common.Application`, not per-service: nothing in its three members names one. `IsAuthenticated` is false on the consumer path, where there is no principal to bind from — which is why [ADR-028](appendix-a-adrs.md#adr-028--a-money-movement-command-carries-no-subject) settles that path by **re-derivation** rather than by binding: §9.6's `AuthorisePayment` carries no `CustomerId` at all, and Payments resolves the payer from its own record of the order — **and false for an anonymous HTTP request**, which carries a principal holding no authenticated identity. Both are "no caller", which is what a guard here has to mean to be a security boundary rather than a null check, and what puts both behind §8.5's one `"system"` segment |
| `CommandOrigin` | §11.4 | `User` \| `System`, saying which path a command arrived on. `User` is the zero value, so an origin nobody set fails closed. Written as a literal at each entry point — never bound from a request or a message |
| `CancelOrderCommand`, `CancelOrderHandler` | §11.4 | The slice both entry paths converge on — HTTP and `CommandConsumer` (§9.4). The command carries a `CommandOrigin`; neither wire shape does. `CancelOrderRequest` is **not** here: it is the HTTP path's wire type and lives in `Ordering.Api.Endpoints` beside the endpoint binding it, the message path's being `CancelOrder` in `Common.Contracts` |
| `CancellationReasons`, `CancellationOrigins` | §11.4, §9.6 | Wire code → `CancellationReason`; the single parse both paths call. **Its sibling goes one way only** — `CancellationOrigin` → a `CancelOrigins` code, and no parse — because no ingress accepts an origin. The asymmetry is the design rather than an omission: a reason is what a caller sends, where an origin is written as a literal at the entry point that knows it, exactly as `CommandOrigin` is, so a `TryParse` here would be the first door onto a value a caller could claim. `CancelOrderHandler` derives one from the other, and anything not provably the workflow is `User` — the direction that makes §9.6's saga fault rather than discard, so a `CommandOrigin` member added later fails loudly instead of arriving as silence |
| `OrderMetrics` | [§13.3](13-observability.md) | Instruments on `Ordering.Orders`. **Application**, not `Common.*`: it takes `Money`. Recorded only from §6.6's projection, on the committed path |
| `ICommandMessageMapper<,>` | §6.2 | Wire contract → application command |
| `IIntegrationEvent` | §9.1 | The three envelope fields every contract carries; the constraint that lets the consumer read `OccurredAt` |
| `ServiceIdentityOptions` | [§15.4](15-cicd-deployment.md) | Bound to `Identity:Client`, `ValidateOnStart`-checked. Registered by the BFF alone (§9.7) |
| `PluggableInterfaces`, `AddPluggableFrom` | §6.2 | The one list of scanned interfaces, and the per-assembly scan that reads it |
| `AddDispatcher` | §6.2 | Registers `IDispatcher`. It exists because `Dispatcher` is internal to `Common.Application`, so no service can write the `AddScoped` line itself |
| `AddDomainEventDispatcher` | §7.5 | Its twin, for the same reason: `DomainEventDispatcher`, `ProjectionRegistry` and `ProjectionRegistryCache` are all internal. Registers the cache as a **singleton** and the other two scoped |
| `IIntegrationEventMapper` | §9.3 | Domain → integration allow-list |
| `IIntegrationEventPublisher` | §9.3 | Stages outbox rows on the current transaction |
| `IProjectionHandler<T>` | §9.4 | Reacts to own events, local lane, after commit |
| `IIntegrationEventHandler<T>` | §9.4 | Reacts to another service's events |
| `OutboxLane` | §9.3 | `Broker` \| `Local` |

## D.2 Domain model — defined in §5

The sample domain. Everything from `Order` down lives in `Ordering.Domain` and
references nothing outside it ([§4.2](04-solution-structure.md)). The first two
rows are the exception and the reason that rule reads "`Common.Domain` and
nothing else": they are shared *mechanism*, they live in `Common.Domain`
([§4.1](04-solution-structure.md)), and they are the one reference a Domain
project is permitted to carry.

| Type | Section | Kind |
|---|---|---|
| `Entity<TId>`, `AggregateRoot<TId>` | [§5.5](05-tactical-ddd.md) | Base types in `Common.Domain`; `Entity` carries identity equality, `AggregateRoot` adds `DomainEvents` and `Version` |
| `IDomainEvent`, `IHasDomainEvents`, `IAggregateRoot` | §5.5 | Domain event contracts and the two non-generic markers, also `Common.Domain` |
| `Order`, `OrderLine` | §5.4 | The aggregate and its child entity |
| `OrderId`, `CustomerId`, `ProductId` | §5.2 | Strongly typed identifiers. `CustomerId` has no `New()`: Ordering never mints a customer, and a factory would be a way to invent a subject (§11.4's rule) |
| `OrderLineId` | §5.2 | A line's own identity, distinct from `OrderId` — `Entity<TId>` compares the type as well as the identifier precisely because a shared key type would otherwise make a line equal to the order holding it (§5.5) |
| `Money`, `Address` | §5.3 | Value objects |
| `OrderStatus`, `CancellationReason`, `CancellationOrigin` | §5.4, §9.6 | Enumerations. `CancellationOrigin` is `User` \| `Workflow` — *who* asked, which `CancellationReason` deliberately does not say: §11.4's endpoint parses the whole `CancellationReasons` map, so a caller may cancel with the saga's own vocabulary and the reason is only what somebody asserted. `User` is the zero value on `CommandOrigin`'s argument, so an origin nobody set reads as "somebody outside this workflow asked" and §9.6's missing-instance branch faults rather than discarding. **Recorded, never enforced** — no invariant in `Order` turns on it; what consumes it is `OrderCancelled.Origin` one layer out. **Every sample in the corpus that constructs one carries it**, reconciled in the change that added it — named that way rather than listed, because the first draft of this sentence enumerated two of the four and the two it missed are the ones a reader meets first: §11.4's handler, which every other site cites as where the origin is decided, and §12.2's TDD narrative |
| `PaymentReference`, `TrackingNumber` | §5.4 | Value objects referenced by aggregate methods |
| `DomainException` | §5.3 | Signals a broken invariant — a bug, not user input (§5.7) |
| `IOrderRepository` | §5.6 | Aggregate persistence port |
| `OrderPlacedDomainEvent`, `OrderCancelledDomainEvent` | §5.5 | Declared in full; carry domain types (`Money`, `OrderLineSnapshot`). **`OrderCancelledDomainEvent` carries a `CancellationOrigin` between `Reason` and `OccurredAt`** (#123), and the position matters to a reader more than it looks: `Order.Cancel` takes the origin and passes it on positionally, so §5.5's declaration and §5.4's `Raise(…)` had to move together with the solution or a reader trusting either would build the wrong record |
| `OrderLineSnapshot` | §5.5 | Immutable copy of a line at the moment an event was raised — events must not alias the aggregate's live list |
| `OrderStockConfirmedDomainEvent`, `OrderConfirmedDomainEvent`, `OrderShippedDomainEvent` | §5.4 | Raised by `Order`; same shape, suffix per §5.5 |
| `V1.OrderPlaced`, `PlacedLine`, `V1.OrderConfirmed`, `ConfirmedLine` | §9.1 | Published event contracts — primitives only, each owning its line type |
| `ReserveStock`, `ReleaseStock`, `StockLine` | §9.6 | Inventory's command contracts. **`ReleaseStock` carries two guarantees the record cannot show**, both in [ADR-024](appendix-a-adrs.md#adr-024--a-release-answers-for-the-order-not-for-the-reservation): it always publishes `StockReleased`, and a release for an order whose `ReserveStock` has not arrived is remembered so the reserve that follows is refused. §9.6's saga meets an early release in `AwaitingStock`, `AwaitingPayment`, `AwaitingConfirmation` and `Confirmed` — on the strength of the first guarantee in those that send a release, and on §3.2's own fan-out in `Confirmed`, which sends none. **It records one rather than absorbing it** ([#143](https://github.com/alexander-shamray/dotnet-ddd-blueprint/issues/143)): each of those states sets `CancellationObserved` on the instance, because a release nobody here asked for is the saga's only evidence that a cancellation reached Inventory, and ignoring it left every forward transition in those states running as though nothing had happened. **The states are written out and not counted** — §9.6's own comment over the same set went stale the moment [#126](https://github.com/alexander-shamray/dotnet-ddd-blueprint/issues/126) split `AwaitingConfirmation` out of `Confirmed` |
| `AuthorisePayment` | §9.6 | Payments' command contract. **Carries no subject** ([ADR-028](appendix-a-adrs.md#adr-028--a-money-movement-command-carries-no-subject)): it decides whose instrument is charged and arrives with no principal, so Payments resolves the payer from its own record of the order. `Amount` and `Currency` stay because they are the **instruction** — what to do, decided by the sender, and checkable against the record as a consistency test — where the subject is the **authority**, on whose behalf, which the deciding service derives. Not a checkability distinction: the record holds the payer too, so a supplied `CustomerId` would be checkable as well; what a transported authority creates is a second source for a decision that must have exactly one |
| `CancelOrder`, `ConfirmOrder`, `MarkOrderShipped`, `FlagOrderForReview`, `CancelReasons`, `CancelOrigins`, `ReviewReasons` | §9.6 | Ordering's command contracts; reason codes are strings, not domain enums. **`CancelOrigins` is the entry that is not a command's vocabulary**: it spells `OrderCancelled.Origin`, an *event's* field, and is listed here because it lives in the file `CancelReasons` does and is read against it. `user` and `workflow` and no third member — §9.6's saga asks the field one question, did this workflow cause the cancellation, so every origin that is not the workflow answers alike; a member per ingress would invite a consumer to switch on it and forget one, and a finer vocabulary belongs on a field read for a finer purpose |

## D.3 Outbox types — two, deliberately

The distinction that finding this appendix's first defect depended on:

| Type | Layer | Columns | Written by | Read by |
|---|---|---|---|---|
| `OutboxMessage` | EF entity, §9.4 | All eleven | `IIntegrationEventPublisher` staging; test fixtures | `db.OutboxMessages`, alerts, purge |
| `OutboxClaim` | Dapper record, §9.4 | The eight the `OUTPUT` clause returns | — | `OutboxDispatcher.ProcessBatchAsync` |
| `MessageTypeMap` | Singleton, §9.4 | Neither — it maps `MessageType` values to types | Built at startup from `MessageTypeSource` | `Stage` on the way in, `DeliverAsync` on the way out, [§12.4](12-test-strategy.md)'s round-trip |
| `MessageTypeSource` | Singleton, §9.4 | The assembly list behind the map, and the persisted-name overrides beside it — `Alias` resolves a name inward and `WriteAs` keeps writing one outward, which is what §9.4's three-release rename needs in both directions | §4.2's registration; §12.4's fixture `Add`s the test assembly | `MessageTypeMap`'s factory |
| `OutboxJson` | Singleton, §9.4 | Neither — the one `JsonSerializerOptions` both ends of the lane use, converters included | Built at startup from the registered `JsonConverter`s | `Stage` and `DeliverAsync`, and §12.4's round-trip test |
| `OutboxTable` | Singleton, §9.4 | Neither — the schema the dispatcher's three statements are composed against | §4.2's registration, one per service | `OutboxDispatcher`'s constructor |
| `InboxTable` | Singleton, §9.5 | Neither — the outbox's sibling, same schema and same database | §4.2's registration, from the **same** schema literal as the pair above | `RetentionPurgeService`'s constructor. Not the filter, which writes through EF and composes no SQL |
| `SqlSchema` | Internal static, §9.4 | Neither — the identifier check and the bracket-quoting the two tables above share | — | Both table types' constructors |
| `RetentionPolicy` | Singleton, §9.4 and §9.5 | Neither — the two windows, the batch size, the interval and the per-pass batch ceiling | §4.2's registration | `RetentionPurgeService`. Registered rather than `const` because §9.5 tells the reader to check the inbox window against their broker |
| `InboxMessage` | EF entity, §9.5 | `(MessageId, Endpoint)` composite key, `HandledAt` | `InboxFilter<T>` | duplicate suppression, retention purge |

`OutboxClaim` has no `ProcessedAt`, `LastError` or `LockedUntil` — a claimed row
is unprocessed by definition, and the dispatcher writes those columns rather
than reading them. Using one type for both produces properties that are
structurally always null on one path.

## D.4 Infrastructure implementations — defined here

| Type | Section | Implements |
|---|---|---|
| `Dispatcher` | §6.2 | `IDispatcher` |
| `EfUnitOfWork` | §6.3 | `IUnitOfWork` |
| `EfDomainEventCollector` | §7.5 | `IDomainEventCollector` |
| `DomainEventDispatcher`, `ProjectionRegistry` | §7.5 | Application ports |
| `ProjectionRegistryCache` | §7.5 | The registry's memo. A **singleton, not a `static` field**: keyed to the container, because a process holding two hosts would otherwise share whichever answer was computed first |
| `RedisIdempotencyStore` | §8.5 | `IIdempotencyStore` |
| `OrderingIntegrationEventMapper` | §9.3 | `IIntegrationEventMapper` |
| `OutboxDispatcher` | §9.4 | `BackgroundService`; `ProcessBatchAsync` is public for tests |
| `ProjectionInvoker` | §9.4 | Cached-delegate handler resolution |
| `MessageTypeMapValidator` | §9.4 | `IHostedService` that resolves `MessageTypeMap` once at startup. It exists because the map is registered through a factory and a factory is lazy — without it the duplicate-name throw lands on a background thread in a ready host, not at boot. Registered before `OutboxDispatcher`, since hosted services start in order |
| `MoneyJsonConverter` | §9.4 | `JsonConverter<Money>` for the Local lane. Per-service, beside the `ComplexProperty` mapping that persists the same value object as columns — a value object with a private constructor deserialises to its default in silence, and the domain may not carry a `[JsonConstructor]` (§4.2's gate) |
| `IntegrationEventConsumer<T>` | §9.4 | `IConsumer<T>` for **events** → `IIntegrationEventHandler`. `Common.Infrastructure.Messaging`, not per-service: nothing in it is, and the binding is what each service owns |
| `CommandConsumer<TMessage,TCommand>` | §9.4 | `IConsumer<T>` for **commands** → the application dispatcher. Same home and the same argument |
| `UnavailableResultException` | §9.8 | Thrown by `CommandConsumer` when a handler returns `ErrorType.Unavailable`, so the endpoint's retry policy sees a fault instead of an ack. `Common.Infrastructure.Messaging`, beside its one producer. The mirror image of `ContractMappingException`: that one exists to be *excluded* from retry, this one to reach it |
| `InboxFilter<T>` | §9.5 | `IFilter<ConsumeContext<T>>` over `DbContext`, `TimeProvider`, `MessagingMetrics` and an `ILogger` — the last two because the suppression branch counts and logs the drop rather than returning in silence (§9.5). Common, reaching the entity through `Set<InboxMessage>()`. Each service registers `AddScoped<DbContext>(sp => sp.GetRequiredService<XDbContext>())`, and the **delegate** is what keeps the inbox row on the handler's context. The row is added **after** `next.Send`, not before: a tracked entity does not survive the `ChangeTracker.Clear()` every command's unit of work opens with (§9.5's trap) |
| `RetentionPurgeService` | §9.4, §9.5 | `BackgroundService` running both purges on one slow schedule; `PurgeAsync` is public for tests. Logs and swallows a failed pass, because a throw out of `ExecuteAsync` stops the host |
| `ProjectedPriceReader` | §6.4 | `IProductPriceReader` over `ordering.ProductPrices` |
| `HttpContextCurrentUser` | §11.4 | `ICurrentUser` over `IHttpContextAccessor`; reads `ClaimTypes.NameIdentifier` and `PermissionClaim.Type`. `Common.Web`, not per-service — it cannot live in `Common.Infrastructure`, which takes no `FrameworkReference`, and `AddCommonWebDefaults` registers it beside `AddHttpContextAccessor()` |
| `PermissionClaim` | §11.4 | The claim type a permission travels in. Four things must agree on it and only three are code — the policies, `HttpContextCurrentUser` and `TestAuthHandler`; the realm's protocol mapper is configuration and is asserted against this constant instead (§11.5) |
| `AuthorizationPolicyExtensions` | §11.4 | `RequirePermission` over `RequireClaim(PermissionClaim.Type, …)`, so no host spells the claim type and an endpoint policy cannot drift from the resource check behind it |
| `AuthenticationExtensions` | [§11.3](11-identity-authorization.md) | `AddJwtAuthentication`, composed by `AddCommonWebDefaults`. Holds `Audience` — a constant, because §11.5 gives the platform one audience and §15.4 disqualifies a value that never varies from being configuration — and `AuthorityKey`, read eagerly with a throw that names it |
| `<Service>Permissions` | §11.4 | Each service's permission vocabulary as constants, at its composition root. The strings are the contract with the realm's claim mapper; the name is written twice — policy and endpoint — so the compiler should be what compares them. `CatalogPermissions.Write` is the first, and its only entry: the listing is anonymous (§10.2), so there is no `catalog:read` |
| `GatewayPermissions` | §11.4, [§10.2](10-api-gateway.md) | The same shape one host over, and the only permission a *route* names rather than an endpoint: `InventoryAdmin`. It lives in `Gateway.Api` because the route that requires it is the gateway's, not `Inventory.Api`'s, and the second site the string is written at is `appsettings.json` — which no compiler reads, so `RouteConfigurationTests` does |
| `RetryAfterHeader` | [§10.3](10-api-gateway.md) | `Seconds(TimeSpan)` — the rejection handler's `Retry-After`, rounded **up** to whole seconds and clamped at zero. A type for one expression because the rule is otherwise unreachable: the gateway's window is a minute long, so every rejection an HTTP test can produce carries tens of seconds and rounds identically whether the code truncates or ceilings. `Gateway.Api`, beside the pipeline that calls it |
| `NoTransformResponseCompressionProvider` | [ADR-020](appendix-a-adrs.md#adr-020--the-edge-compresses-over-tls-and-says-so) | `Gateway.Api`; a `ResponseCompressionProvider` subclass adding one case in front of `ShouldCompressResponse` — neither side of the exchange may carry `Cache-Control: no-transform`. RFC 9111 §5.2.2.6 requires that of an intermediary for the response form and ASP.NET Core does not implement it, so that half is conformance rather than policy; §5.2.1.6 makes the request form an ask rather than an obligation, and it is honoured anyway — which is why the provider also appends `Cache-Control` to `Vary` on every decision, the representation now depending on a request header that no cache would otherwise key on — skipping that when the response already varies by it or has declared the `*` wildcard, which the framework's own `Accept-Encoding` entry does not check for. Registered with `Replace`, because `AddResponseCompression` uses `TryAddSingleton` and ordering would otherwise decide it silently |
| `GatewayLimits` | [§10.1](10-api-gateway.md), [ADR-020](appendix-a-adrs.md#adr-020--the-edge-compresses-over-tls-and-says-so) | `MaxRequestBodyBytes` — one mebibyte, and the platform's only body ceiling. A constant rather than configuration because it does not vary between environments (§15.4), and a named one rather than a literal in `Program.cs` because the tests either side of the boundary spend it — which is what lets them assert the boundary's semantics rather than a value, so the number can move without touching them |
| `GatewayRateLimiterPolicies` | [§10.3](10-api-gateway.md) | `Anonymous`, `Authenticated` and the `All` list beside them. Not the authorization policy of the same name — §10.2 keeps two registries apart under one word. The list exists because the limiter's policy map is internal to the framework, so nothing can ask a built host what it registered: YARP refuses to load a route naming a policy that is absent, and `All` is what catches the other direction, a policy registered and named by nothing |
| `SensitiveDataRedactor` | §13.4 | `BaseProcessor<LogRecord>` on the pipeline §13.2 builds; enforces `SensitiveKeys` over a record's **attributes** — by key, and by value for the two shapes `LooksLikeSecret` recognises, so a connection string reaches the collector redacted whatever its key was called. A match rewrites more than the attribute: `IncludeFormattedMessage` is on, so the exporter ships `FormattedMessage` as the body and it falls back to the un-substituted template, and the exception is **dropped** when its text repeats a value just redacted, because OTLP serialises `exception.message` separately from both. `{OriginalFormat}` is exempt from the value check — the template is written by an author rather than bound from data, and redacting it would remove the one attribute that fallback depends on. **It no longer owns the list**: two types read it and the copy nobody edits is the one that stops matching. Scopes were never its to fix — a `LogRecord` exposes `ForEachScope` and no settable provider, so reading one here would let it *notice* a secret it has no way to remove |
| `SensitiveKeys` | §13.4 | The never-log vocabulary, declared once because two things read it: `SensitiveDataRedactor` for a record's attributes and `RedactingScopeProvider` for the scopes those records inherit. Matching is by substring, ordinal and case-insensitive, because the field that leaks is never named exactly `password` — it is `NewPassword`, `card_number`, `id_token`. The cost is that a term inside an innocent word redacts it too, which is why `pin` is deliberately absent: `Shipping` contains it. `LooksLikeSecret` is the value half and the half that survives a key nobody predicted — a connection string carrying `Password=` inline and a JWT's compact serialisation, two shapes that are unmistakable and that this platform demonstrably holds. Deliberately **not** an entropy test: a high-entropy string is an id as often as a credential, and redacting every id empties the records §13.1 has an incident triaged by. **Public so a test can pin it**, on `SensitiveDataRedactor`'s own terms — the list is the control, so a term removed in a refactor must fail a test rather than silently widen what is exported |
| `RedactingScopeProvider` | §13.4 | `IExternalScopeProvider` wrapping the one `LoggerFactory` hands every provider, which is where §13.4's rule reaches log **scopes** — the channel `SensitiveDataRedactor` can read and cannot rewrite. `IncludeScopes` is on (§13.2), so every record carries the scopes open around it, and the wrap covers EF Core's and MassTransit's as well as `LoggingBehavior`'s `RequestType` (§13.3) and `UseCorrelationId`'s `CorrelationId` ([§10.4](10-api-gateway.md)). That reach is the point: the argument this replaces held the platform's own two scopes to be provably safe, and one of them was carrying a client-supplied header at the time. Redacts on the way **out** — `Push` stores the caller's object untouched, because a scope is also a live object the application may read back, and only the enumeration a logging provider performs is rewritten. Matches `IEnumerable<KeyValuePair<string, object?>>` and **not** the read-only list: `BeginScope(new Dictionary<,>)` is what §10.4 and §13.3 both open and a `Dictionary` is not an `IReadOnlyList`, so matching the list interface alone left every scope this platform actually opens unredacted while the unit tests over MEL's own type stayed green. The scrubbed copy overrides `ToString`, because a provider that formats a scope rather than enumerating it would print the secret straight back out of the list just scrubbed |
| `OutboxMetrics` | §13.6 | Three observable gauges on the `Ordering.Outbox` meter, one measurement per lane; singleton, eagerly constructed. The per-lane measurements are read from `Enum.GetValues<OutboxLane>()` rather than written out at each of the three call sites, so a lane added and forgotten cannot become a lane with no gauge and therefore no alert |
| `RequestMetrics` | §13.3 | `request.duration` on `Commerce.Requests`; injected by `LoggingBehavior` and still forced by `MetricsInitialiser` (§13.6) — a health probe never enters the pipeline |
| `MessagingMetrics` | §13.3 | Four instruments on `Commerce.Messaging`: the `messaging.delivery.lag` and `projection.lag` histograms, the `command.domain_rejected` counter `Rejected` writes (§9.8), and the `messaging.inbox.suppressed` counter `Suppressed` writes (§9.5). Injected by `IntegrationEventConsumer<T>`, `CommandConsumer<,>` and `InboxFilter<T>`; resolved from the provider by the static `ProjectionInvoker`. **The class grew in instalments** — `Projected` landed with the outbox, the next two with the consumers that record them, and the fourth when §9.5's silent drop was given a signal — on `PluggableInterfaces.All`'s terms |
| `IOutboxStats`, `OutboxStats` | §13.6 | Backlog age, pending count and abandoned count, each per lane. **No scope**: §6.5's `IDbConnectionFactory` is a singleton holding a string, so it is injected directly and the reads are Dapper on a connection the caller disposes — §13.6 reached for an `IServiceScopeFactory` until PR-24 measured what it was resolving. Cached five seconds, because an observable gauge is read once per export interval per instrument and a metrics type that loads the database it measures is a monitor causing the symptom. The abandoned query reads `OutboxDispatcher.MaxAttempts` rather than repeating the number |
| `MetricsInitialiser` | §13.6 | `IHostedService` whose only job is forcing the metrics singletons to be constructed. Three parameters, not four — `OrderMetrics` joins with §6.6's `OrderSummaries` projection — and each is passed to `ArgumentNullException.ThrowIfNull`, because an unread primary-constructor parameter is CS9113 and ADR-019 makes it an error |
| `OrderFulfilmentState` | §9.6 | `SagaStateMachineInstance`; persisted to `ordering.OrderFulfilmentStates`. **Two of its columns are a join and not a record of what happened**: `PaymentVerdictOutstanding` and `StockReleaseSettled` are what let `Compensating` wait for both of the answers it is owed instead of finalising on whichever arrived first ([#124](https://github.com/alexander-shamray/dotnet-ddd-blueprint/issues/124)). Neither is derivable from `CurrentState` — that state is reached by several routes and only some of them leave a verdict outstanding — and `StockReleaseSettled` is not `ReleaseTimeoutTokenId` under another name, because `Unschedule` clears the token on one stock exit and leaves it standing on the other, so a null test would answer for one settled route and not both. A timeout deliberately does not clear the verdict: it ends the wait, not the obligation. **It no longer declares a `CustomerId`** ([ADR-028](appendix-a-adrs.md#adr-028--a-money-movement-command-carries-no-subject)): the instance carried one for a single reader, the `AuthorisePayment` that named the subject Payments would charge, and with that field gone all a copy here could do is offer itself to the next transition that wants a customer. The **column** outlives the property by one release — §15.5 forbids dropping what the release beside this one still writes — so it is mapped as a shadow property with a conservative default until a later release drops it. **`CancellationObserved` belongs on the other side of the sentence this cell opens with**: it is a record of what happened rather than half of a join ([#143](https://github.com/alexander-shamray/dotnet-ddd-blueprint/issues/143)) — a `StockReleased` arriving in a state that sent no `ReleaseStock`, which is Inventory acting on an `OrderCancelled` the saga has not consumed yet ([ADR-029](appendix-a-adrs.md#adr-029--inventory-releases-on-the-cancellation-not-on-the-sagas-word)). Every forward transition that could do harm on a cancelled order asks it, and **it is never cleared**: a cancellation does not stop being in flight, and the states that read it either compensate or finalise, so a reset would be a way to lose the fact rather than a way to use it. Mapped by convention as `bit NOT NULL DEFAULT 0`, exactly as `PaymentVerdictOutstanding` and `StockReleaseSettled` are. It narrows [#143](https://github.com/alexander-shamray/dotnet-ddd-blueprint/issues/143)'s window and does not close it — a cancellation Inventory has not consumed yet leaves no trace here at all |
| `ProductPriceProjection` | §6.6 | Writes `ordering.ProductPrices` and `ordering.ProductWithdrawals` from Catalog's three product events, on the `ordering-catalog-events` endpoint (§9.8). **Public**, because §6.2's scan is public-only and an internal handler registers as nothing while the endpoint stays bound. Its `MERGE` carries `WITH (HOLDLOCK)`: without it two concurrent deliveries for one key can both insert, which the endpoint's retry would absorb rather than surface |
| `ProductWithdrawal` | §6.6 | The product-level watermark the upsert consults on its `NOT MATCHED` branch. Exists because `ProductDiscontinued` carries no currency and `ProductPrices` is keyed by one, so a withdrawal cannot reach a row that does not exist yet — and §9.4 guarantees no ordering. A **watermark**, not a flag: a later republish re-lists the product |
| `OrderSummaryProjection` | §6.6 | Writes **two** tables: `ordering.OrderSummaries` from Ordering's five lifecycle events on the local lane, and `ordering.Products` from Catalog's `ProductPublished` off the broker. **Two streams, two tables, and that is what makes two watermarks possible**: `UpdatedAt` on a summary versions the lifecycle sequence, `UpdatedAt` on a product versions that product's own, and the two sets of statements do not overlap. The tables are split rather than the columns because the sequences are keyed differently — an order has as many `ProductPublished` sequences as it has products, so any single column on the summary versions many streams at once and discards all but the newest ([ADR-027](appendix-a-adrs.md#adr-027--the-order-summary-stores-product-ids-and-resolves-the-name-locally)) |
| `PriceChangedCacheInvalidator` | §8.4 | Ordering's second `PriceChanged` handler — cache only |
| `ClientCredentialsHandler` | §11.5 | `DelegatingHandler`; attaches the M2M bearer token to the BFF's outbound calls. Registered **after** the resilience handler so it sits inside it, so that **each retried attempt re-attaches a token** rather than replaying the first one. The retries that fire are transport faults (§9.7), not statuses — so this matters whenever an attempt is repeated at all, and a token that expired between attempts is what it saves |
| `PricingService`, `ValidationInterceptor` | §9.7 | Catalog's server half of the pricing hop, and the interceptor turning a `ValidationException` into `InvalidArgument`. Without the second, a malformed request reaches the caller as `Unknown`, which the BFF maps to a 500 — a caller's bad input reported as the platform's fault. Not a retry concern: a gRPC status rides an HTTP 200 and the resilience pipeline never sees it |
| `PricingHop` | §9.7 | The BFF's client name, resilience options name and Catalog's address, in one place. Port **8081**, not 8080: a cleartext Kestrel endpoint cannot serve HTTP/1.1 and h2c at once |
| `UpstreamExceptionHandler` | §9.7 | `IExceptionHandler` in `Web.Bff`; `RpcException` to a status a caller can act on — `InvalidArgument` to 400, `Unavailable`/`DeadlineExceeded` to 503, and `Internal` to 503 **when `Status.DebugException` is one of the three the pipeline itself raises**, because a failure inside the client has no gRPC status and arrives as `Internal` carrying its cause. Everything else is left **unhandled** so it stays a logged 500 rather than a quietly reclassified one |
| `ServiceOptions` | §15.4 | Static constants in `Common.Web` — not bound, not validated, not deployable; `OperationTimeout` is the ceiling §9.7's hierarchy asserts against. Listed **here** rather than in D.1: it is not an application port, and it was briefly in both |
| `ValidationBehavior<,>`, `TransactionBehavior<,>` | §6.3 | `IPipelineBehavior<,>` |
| `LoggingBehavior<,>` | §13.3 | `IPipelineBehavior<,>`; outermost — pushes the request scope, logs the outcome and records `request.duration` |
| `IdempotencyBehavior<,>` | §8.5 | `IPipelineBehavior<,>` over `IIdempotencyStore` and `ICurrentUser` — the second scopes the claim to a subject. Holds the claim token its `TryClaimAsync` returned and passes it to both writes, which is what lets the store refuse one from an attempt that has lost the key. Constrained `TResult : Result`, which the container enforces by omission |
| `UseCorrelationId`, `MapCommonHealthEndpoints`, `AddObservability`, `AddCommonWebDefaults` | §10.4, §13.5, §13.2 | `Common.Web` host extensions. `MapCommonHealthEndpoints` takes one parameter and it is a written decision: an empty predicate set is a passing predicate set, so the default **throws at startup** when no `ready`-tagged check is registered, and the gateway and the BFF — the two hosts §13.5 says own no *readiness* dependency, which is not the same as owning none: the BFF calls Catalog (§9.7) and the gateway proxies four services, and neither hop gates readiness — pass `ownsNoReadinessDependencies: true`. `AddCommonWebDefaults` sets the fallback authorization policy of [ADR-030](appendix-a-adrs.md#adr-030--authorization-is-deny-by-default-in-the-building-block) and takes no parameter to opt out of it, because a parameter is a way to turn the rule off |
| `SecurityHeadersExtensions` | §10.6, [ADR-031](appendix-a-adrs.md#adr-031--the-service-owns-nosniff-the-ingress-owns-hsts) | `Common.Web`; `UseSecurityHeaders`, called outermost by all four hosts (§4.2) — the edge is not in front of the other three from inside the cluster (§11.2), so a header set only at the gateway is absent on every path that does not traverse it. **The list is one entry**, `X-Content-Type-Options: nosniff`, and the omissions are argued rather than owed: HSTS belongs to the Ingress, the only component here that terminates TLS (§10.1, [§15.3](15-cicd-deployment.md)), and a host behind it would be asserting something it cannot observe; `X-Frame-Options` and `Content-Security-Policy` govern how a browser renders a *document*, and no host here serves one — the API responses are `application/json` or `application/problem+json` and §13.5's probes are `text/plain`. Written from an `OnStarting` callback rather than before `next`, because `UseExceptionHandler` clears the response before writing §10.5's problem body — a header assigned on the way in is gone from exactly the 500 on which a caller-supplied value is most likely to be reflected. A second header needs the ADR amended |
| `CorrelationIdHandler` | §10.4 | `Common.Web`; the `DelegatingHandler` carrying `X-Correlation-Id` onto an outbound call. The middleware is the inbound half and writes the ID no further than this process — events take it in §9.1's envelope, and the one synchronous hop (§9.7, ADR-017) had nothing until this. Registered by the host rather than by `AddCommonWebDefaults`, because a handler attaches to a named client. Sends **no** header when there is no inbound ID, so the callee mints one from its trace rather than reading a blank |
| `AddCommonProblemDetails` | §10.5 | `Common.Web`; the RFC 9457 customisation `AddCommonWebDefaults` composes — and the registration of all **three** exception handlers below, so no host takes one part without the others. **Three since §8.5's PR, and this cell said two**: a count beside a list is the half that rots, because adding a handler is one line in the method and one row here, and nothing pairs them |
| `ValidationExceptionHandler` | §10.5 | `Common.Web`; the `IExceptionHandler` executing the table's 400 row — `ValidationBehavior`'s thrown `ValidationException` into field-keyed `errors`, registered by `AddCommonProblemDetails` |
| `ConcurrencyExceptionHandler` | §10.5 | `Common.Web`; the `IExceptionHandler` executing the table's 409 row — `DbUpdateConcurrencyException` into a problem response naming neither entity nor row version. Matches the derived type, not `DbUpdateException`, which also covers a constraint violation and is not a race |
| `ConcurrentRequestExceptionHandler` | §10.5 | `Common.Web`; the **second** producer of that 409 — `ConcurrentRequestException` (§8.5) into *a request under this key is still in flight*. Reachable only from the PR that gave `IdempotencyBehavior` its pipeline seat; before it the 500 fallback answered, reporting the mechanism working as a server fault. Echoes no `CommandId` |
| `ToHttpResult` | §10.5 | `Common.Web`; extension on `Result` and `Result<T>` — 204 or the value, and `ErrorType` to a status otherwise |
| `OrderFulfilmentSaga`, `Endpoints` | §9.6 | Saga and its command destinations. `Endpoints` is `internal` where `CatalogEventsQueue` beside it is public, and the two answer different questions: this holds `queue:` addresses the saga **sends** to and nothing outside the assembly sends, while a queue *name* is what an inbox row records and therefore what a test must be able to spell |
| `StockReservationExpired`, `PaymentAuthorisationExpired`, `ConfirmationExpired`, `DespatchExpired`, `StockReleaseExpired` | §9.6 | The scheduled messages, one per wait state. **Not contracts and deliberately not in `Common.Contracts`**: they appear in no column of §3.2, the saga sends them to itself, and putting them where §4.3's one crossing assembly lives would advertise a set of messages a peer could legitimately publish — which is how a saga acquires a way to be driven from outside. One record per wait rather than one carrying a discriminator, because MassTransit correlates a schedule by message **type**: a single type makes every schedule one subscription, and a stale expiry could not then be told apart from a live one. **Not "so `Unschedule` knows which token to cancel"** — on ADR-021's scheduler `Unschedule` cancels nothing, so what discriminates an expiry is its type against the state it arrives in. **That discrimination stopped being theoretical with [#124](https://github.com/alexander-shamray/dotnet-ddd-blueprint/issues/124)**, which gave `PaymentAuthorisationExpired` a second receiving state: in `AwaitingPayment` it ends the payment wait and arms it once more on the way into `Compensating`, and in `Compensating` it stops asking for good and lets the instance finalise if the stock half has settled. Same type, opposite meanings, told apart by nothing but the state it lands in — which is the sentence above doing work rather than describing. **The counts are gone from this cell rather than corrected**: it said "the four" three times over and #126 falsified all three at once, where the invariant a reader wants is one per wait state — which `Every_wait_state_declares_a_schedule` asserts against the machine. `ConfirmationExpired` is the only one whose far end is Ordering itself |
| `CancelOrderMapper`, `ConfirmOrderMapper`, `MarkOrderShippedMapper`, `FlagOrderForReviewMapper` | §9.4, §9.6 | `ICommandMessageMapper<,>`, one per command in §3.2's Accepts column. All four parse the wire vocabulary, and all four throw `ContractMappingException` rather than letting a `DomainException` reach the handler — so a payload this service cannot read goes to the error queue on the first attempt instead of being acked as a domain rejection. **Only `CancelOrderMapper` declares `CommandOrigin.System`**, because only `CancelOrder` has a second way in: §11.4's ownership check exists to tell a customer's cancellation from the saga's, and the other three have no second ingress to be told apart from — all four are bound on `ordering-commands`, and only `CancelOrder` also has an HTTP route. The stamp lives in the mapper rather than on the wire contract, so nothing a peer *sends* can claim it — what earns it is arrival on the queue, which is only as strong as broker authorisation (§9.4's callout) |
| `StockReservedHandler` | §9.6 | `IIntegrationEventHandler<StockReserved>` in **Application**, on the `ordering-stock-events` endpoint (§9.8). Ordering's second reader of that event — the saga is the first — and it **dispatches** `ConfirmStockCommand` rather than mutating the order, because work done inside an integration-event handler commits through the inbox filter's `SaveChangesAsync` and stages nothing, so the domain event would be dropped in silence (§7.5) |
| `FlagOrderForReviewHandler` | §9.6 | `ICommandHandler<FlagOrderForReviewCommand, Result>`, printed in full by the chapter — which is why it is here rather than in D.5 beside the two saga handlers that are not. Writes the `OrderReviews` row through `IUnitOfWork.ExecuteRawAsync` and loads no aggregate — because the row is an operations fact rather than a domain one. **The aggregate’s state is not the reason either way**, and both this cell and its predecessor argued from it — the version before this one said the order is not yet cancelled when `payment_authorised_during_compensation` is raised, which [#124](https://github.com/alexander-shamray/dotnet-ddd-blueprint/issues/124) made unreliable in both directions. `CancelOrder` is sent by whichever of `Compensating`’s stock exits runs, and that exit may now run before the row is raised or after it, because the state finalises on a join rather than on the stock half alone. So the order may or may not have been cancelled by then, and a handler that loaded the aggregate would find either — which is the point: it does not read one |
| `OrderReview` | §9.6 | A row of `ordering.OrderReviews` — mapped by EF so the migration owns the schema, written by Dapper through `IUnitOfWork.ExecuteRawAsync`, and given no `DbSet` so nothing acquires a second write path around the conditional insert's range lock. Deliberately not a domain type — because the row is an operations fact rather than a domain one. **The aggregate’s state is not the reason either way**, and both this cell and its predecessor argued from it — the version before this one said the order is not yet cancelled when `payment_authorised_during_compensation` is raised, which [#124](https://github.com/alexander-shamray/dotnet-ddd-blueprint/issues/124) made unreliable in both directions. `CancelOrder` is sent by whichever of `Compensating`’s stock exits runs, and that exit may now run before the row is raised or after it, because the state finalises on a join rather than on the stock half alone. So the order may or may not have been cancelled by then, and a handler that loaded the aggregate would find either — which is the point: it does not read one |
| `ServiceFixture` | §12.4 | Testcontainers `IAsyncLifetime` fixture; owns the `WebApplicationFactory`. One per service, in that service's `*.TestSupport` (§4.1) — Catalog's is the first built, and §12.4's worked example is Ordering's |
| `TestAuthHandler` | §12.4 | `AuthenticationHandler<AuthenticationSchemeOptions>`; issues the principal a test names in headers, with `PermissionClaim.Type` on each granted permission so a test exercises the policy rather than bypassing it. Its constant is `SchemeName`, not `Scheme` — the base class already declares one, and CS0108 is an error under ADR-019. Per **suite owner**, not per service: each service keeps its copy in `*.TestSupport`, and the gateway's lives in `Gateway.Api.Tests` itself, which has one suite and therefore no TestSupport library ([§4.1](04-solution-structure.md)). The copies are deliberate — §4.3 permits one assembly to cross a boundary and it holds integration events — and what they share is `PermissionClaim.Type`, read from `Common.Web` |
| `StubCatalog` | §12.4, §12.6 | The BFF suite's stub: a real gRPC server on an ephemeral loopback port, answering prices, refusing with a queued `StatusCode`, or **aborting the connection**. The last is not a variant of the second — a gRPC status rides an HTTP 200 with `grpc-status` in the trailers, so only a transport fault reaches §9.7's retry. It lives in `Web.Bff.TestSupport` because it compiles the server half of a contract `Web.Bff` already compiles the client half of, and two copies in one compilation is CS0436. Since PR-26 it is **held to `PricingContract`** rather than trusted: `Publish` realises an interaction's state, `Replies` records what was sent so the same verification the provider run applies can be applied to it, and the four behaviours that had drifted from Catalog — a case-sensitive currency filter, the request's spelling echoed instead of the stored one, an amount at the test's scale instead of the column's `decimal(19,4)`, and no request ceiling — are fixed. Its currency is **per product**, as Catalog's is; one currency for the whole catalogue could not express a basket that fills `QuoteResponse.Unpriced` while still totalling the rest |
| `PricingContract` | §12.6 | `Web.Bff`'s expectations of §9.7's hop — six interactions, the ids and currency each one asks about, and the consumer's per-reply tolerance, which is applied to the stub's reply and to the real Catalog's alike. It also builds the `GetPricesRequest`, and **only the provider suite sends that one**: the consumer suite passes the same ids to the screen as a query string so that `CheckoutEndpoints` builds its own, which is the thing that half exists to check. It lives in `Web.Bff.TestSupport` because only a consumer can author one, and is **linked** into `Catalog.Api.Tests` rather than referenced, exactly as `pricing.proto` is linked into `Web.Bff` — a file and not an assembly, so §4.3 keeps `Common.Contracts` as its one exception. Not Pact, and the mechanism is [ADR-023](appendix-a-adrs.md#adr-023--the-consumer-driven-contract-is-a-linked-file-not-pact)'s decision rather than a convenience |
| `PricingInteraction`, `ContractProduct`, `PricingOutcome`, `PricingContractException` | §12.6 | The contract's shape. An interaction names its products by **alias**, because neither side can be handed an id — the stub mints its own and the provider suite is given Catalog's — so each realises the state and binds the alias. `PricingOutcome` is a closed hierarchy over `Priced` and `Refusal`: one record carrying both a list and a status would admit a third state that means nothing. The exception is the file's own, on CA1032's three constructors, because the same file compiles into a library with no assertion package and into a suite with Shouldly |
| `StubDestination`, `StubbedGatewayFactory` | §12.4 | The gateway suite's pair: a real Kestrel server on an ephemeral loopback port answering 204 — or, asked through a query string, a compressible body, optionally under a declared `Content-Encoding` of `gzip` or `identity` (the double-compression guard, which refuses both alike) or under `Cache-Control: no-transform` (ADR-020's actual opt-out) — and recording the path it was given, with the factory pointing every cluster at it. A listener rather than an address that refuses — a refused connection measured about two seconds a request, so §10.3's hundred-request window replenished before a test could exhaust it — and the recorded path is the only observation of the prefix strip anywhere in the solution |
| `TestCurrentUser`, `Principals` | §12.4 | The scoped `ICurrentUser` double and its three values — `Default`, `Authenticated(subject, permissions)` and `Anonymous`. It **delegates** rather than replacing: unset inside a request it resolves through `HttpContext` exactly as production does, so `TestAuthHandler` keeps driving the endpoint tests; unset below HTTP it is `Default`; set, it is whatever the test said. A flat double would break the ownership-404 test by passing for the wrong reason |
| `ServiceFixture.DispatchAsync` | §12.4 | Opens a scope, points its `TestCurrentUser` at a named principal and dispatches; one overload each for `ICommand<T>` and `IQuery<T>`. The seam the subject tests use to reach a handler with no caller — which HTTP cannot produce against an endpoint group carrying `RequireAuthorization` |

## D.5 Referenced but deliberately not shown

These are named in samples and left undefined on purpose. They are ordinary,
and writing them out would add length without insight.

**The rule: any project-specific name a sample references — type *or* member —
that is not defined in D.1–D.4 must appear here.** If it is in neither, it is a
defect rather than an omission. Framework and BCL names (D.6) are out of scope;
so are members of types the document does define, since those are visible at
their declaration.

| Name | What it is |
|---|---|
| `IDbConnectionFactory`, `SqlConnectionFactory` | Creates a `SqlConnection` for Dapper reads — closed; Dapper opens it, the caller disposes it |
| `OrderingDbContext` | The service `DbContext`; configuration in §7.2 |
| `OutboxPublisher` | `IIntegrationEventPublisher` writing `OutboxMessage` rows; resolves `MessageTypeMap` and `OutboxJson` and hands both to `Stage`. Mints one correlation id per scope, lazily — a scope is one command, so rows staged together correlate, and an integration event overrides it from its envelope (§9.1) |
| `Result`, `Result<T>` | Non-generic `Result` is the void case — there is no `Unit` — and `Result<T>` derives from it, which is what lets `TransactionBehavior` test any command's outcome with one pattern (§6.3). `IsSuccess`/`IsFailure`, `Error`, `Result<T>.Value`, and the `Success`/`Failure` factories. **The state invariant is structural rather than access-controlled**: `IsSuccess` is *defined* as the absence of an error, so no result reports success while carrying one, whatever built it. The non-public constructors buy something else, and it is what §8.5 needs — `Result`'s is `private protected` and `Result<T>`'s `internal`, so nothing **outside** `Common.Application` can construct one at all, a serialiser included. The factories buy the third thing: `Failure(null)` is refused rather than silently answering success. `Error` and `Value` are non-nullable and **throw** on the wrong state rather than answering null, which is why §8.5 stores a value rather than a serialised `Result`. Measured against the shipped type: of its four states it **serialises in one** — the non-generic failure, the only one whose accessors all answer — and **deserialises in none**, there being no constructor a serialiser may call |
| `CursorPage<T>`, `Cursor` | The pagination envelope and the opaque cursor codec (§6.5) |
| `AddRedisConnections`, `AddMassTransitMessaging` | Infrastructure registration helpers. The first is `Common.Infrastructure`'s one entry point, shown abbreviated in §8.2 — connections, cache stack, `RedisKeys`, the lock factory, the Redis tracing instrumentation and — since §8.5's PR — `IIdempotencyStore` bound to `RedisIdempotencyStore`, in one call. The store is registered there rather than per service on the helper's own argument: the port is `Common.Application`'s and the connection it needs is the one this method establishes, so a service that has Redis has the store and one that does not cannot half-have it. The second is **per-service** (`{Service}.Infrastructure.Messaging`, the `Redis/DependencyInjection` shape one tree over): reads `ConnectionStrings:RabbitMq` eagerly and throws naming the key, disables MassTransit's usage telemetry, configures the RabbitMQ bus — and registers no health check, because `AddMassTransit` contributes `masstransit-bus` itself (§13.5). Per-service because it is where each service's consumers, sagas and receive endpoints are configured (§9.6, §9.8) |
| `RedisConnections` | Keyed-service names for the cache and coordination connections (§8.1), spelled like the configuration keys they resolve from |
| `RedisKeys` | §8.3's naming authority: `Lock`, `Idempotency` and `Denylist` return the full `ApplicationName`-prefixed key, and `CacheInstanceName` is the cache half's prefix. Deliberately no `Cache(string)` — the double-prefix hazard §8.3 names |
| `IDistributedLockFactory`, `IDistributedLock` | §8.1's lock and its held handle: `TryAcquireAsync(name, duration, ct)` returns null under contention, the TTL is mandatory, and disposal is the token-checked release |
| `BuildInfo` | Assembly version stamped onto OTel resource attributes (§13.2) |
| `OrderBuilder`, `AddressBuilder`, `CommandBuilder`, `SeedData` | Test data builders (§12.3) |
| `OutboxRows` | Outbox test builders (§12.4) — `Poison`, `Healthy`, `Unhandled` and `Verbose`, one class rather than one per case. Each takes the **fixture**, not the map alone: a staged row needs both halves of the host's agreement about the format, and one written without the service's converters round-trips to a defaulted value object. `Raised` is a fixed instant, because `OccurredAt` is what §13.7's lag is measured from |
| `Contracts`, `ContractSamples` | Test builders for `required`-member contract messages — one sample per contract type, and the reason the §12.6 suite cannot silently skip a new one. `Sampled` exposes the registry's keys, so the suite can also fail a sample whose contract has gone |
| `RecordedMeasurements`, `TestMeterFactory` | A `MeterListener` scoped to one meter, and the ten-line `IMeterFactory` behind a metrics class under test. What §9.8 and §13.3 constrain is the instrument's **name and tags**, which a substituted metrics type could not assert |
| `RouteConfiguration.ReadAll`, `RouteConfigurationTests.ReadRoutes` | One route of `ReverseProxy:Routes` and the two readers §12.4's samples call. **`ReadAll` is the type's static reader; `ReadRoutes` is the suite's private wrapper and not an alias for it** — it adds the non-empty assertion every other test in the class rests on, since each of them is a `foreach` and a `foreach` over nothing passes. Both are read through the host's own `IConfiguration` rather than off disk: that is the exact text YARP binds, where a second parser would assert against itself, and the file legally carries comments that only the configuration provider's `JsonCommentHandling.Skip` is guaranteed to agree with. `RouteConfiguration` carries the two derived values the assertions need — the forwarded path prefix, and the namespace the match sits under |
| `AlwaysThrows`, `NoOpEvent`, `UnhandledEvent` | Test event types with throwing / no-op / no handler |
| `IntegrationCollection` (xUnit) | The `[CollectionDefinition]` sharing one fixture across test classes — declared once **per test assembly** (§12.4), over `ServiceFixture` in a service's suites and over `RedisFixture` in `Common.Infrastructure.Tests`. Named for what it groups, and deliberately not `ServiceCollection`: that name is taken by `Microsoft.Extensions.DependencyInjection`, and the local type would win in every test file that also builds a provider. It also carries `[Trait("Category", "Integration")]`, which xUnit v3 applies to every test in the collection — so the declaration that grants a test its containers is the same one that puts it in the slow half, and there is no per-class attribute to forget. `Web.Bff.Tests`'s `KeycloakCollection` is the same decision under another name, over the one fixture that suite shares — three fixtures, one shape |
| `OrderRepository` | `IOrderRepository` over EF; also the Infrastructure assembly marker for the §6.2 scan |
| `ContractMappingException` | Thrown by an `ICommandMessageMapper` on a value it cannot map; ignored by the retry policy so it reaches the error queue immediately (§9.4, §11.4) |
| `Publish` | §12.5's fencing publish: publishes, then waits for **that message id** to be consumed before returning, so two consecutive publishes cannot race. The barrier is in the helper rather than at the call sites because per-site discipline fails open — the test that forgets is the test that flakes, and only on a loaded runner. On the send context's id, which for a contract is the envelope's own — the helper writes it there (§9.1), so the harness does not mint the second identity `IIntegrationEvent` forbids. A type-level wait matches the first delivery and fences nothing wherever a suite delivers one type twice; two deliveries of *one* message share an id by design and are §9.5's problem, not the barrier's |
| `StartHarnessAsync` | Test helper returning the provider and a started MassTransit harness — the provider because the suite disposes it — and where a saga suite states both harness bounds — which a **positive** assertion waits under, since §12.5's negatives read the record on a cancelled token and wait for nothing |
| `KeycloakFixture`, `ClientCredentialsAsync`, `CreateUnrelatedClientAsync`, `ServiceValidatingTheRealm` | §11.5's suite: the Keycloak container, a client-credentials grant against it, and a minimal host running the platform's real `AddJwtAuthentication`. **No Catalog container** — what is under test is the registration plus the realm, and neither is Catalog's, so a service would add a SQL container to a suite whose subject is a token. Audiences are read with `JwtSecurityTokenHandler` |
| `MessageTypes` | Fixture handle on the real `MessageTypeMap` (§12.4). **Not `Types`**: that name belongs to `NetArchTest.Rules.Types`, which §4.2's architecture tests call as `Types.InAssembly(...)`, and a fixture member would shadow it in any file holding both — the `ServiceCollection` collision again, two rows above |
| `Types`, `Json` | A `MessageTypeMap` over the contract and test assemblies and an `OutboxJson` with no converters, built once as private statics in the unit tests (§12.4). Not the fixture's — the `Stage` test takes no fixture, which is the point of it |
| `OutboxJson` (fixture member) | Fixture handle on the registered `OutboxJson` (§12.4), so a staged row and a delivered one agree about converters as well as settings |
| `DomainEventSamples` | One sample per stageable domain event, so a new event without one fails §12.4's round-trip rather than being skipped — `ContractSamples`' counterpart for the Local lane |
| `ConcurrentRequestException` | Thrown when an idempotency key is claimed but unfinished (§8.5) |
| `InProgressMarker` | The state `TryClaimAsync` writes while a command is in flight; `GetAsync` reads it back as `InProgress` (§8.5). Not the whole stored value since the claim token landed — the value is `{claim}:{state}`, and the marker is the state half. It stays deliberately invalid JSON so no serialised payload can spell it |
| `InvariantViolationException` | Thrown when a command modifies more than one aggregate root (§6.3, principle 3) |
| `ITokenCache`, `CachingTokenClient` | Acquires and caches the M2M access token until shortly before expiry; BFF-only (§11.5) |
| `PriceRow` | Dapper row shape behind `ProjectedPriceReader` (§6.4) |
| `SummaryRow` | Dapper row shape behind the escalated summaries query, `Products` still JSON (§6.6) |
| `FulfilmentFact` | Dapper row shape behind §6.6's fulfilment claim — `(PlacedAt, ConfirmedAt)`, non-nullable because the claim's predicate tests both columns for `IS NOT NULL` |
| `PlacedFact` | `(TotalAmount, Currency)` behind the placement claim. Non-nullable for a **weaker** reason: the predicate tests `PlacedAt`, and these are non-null only because the `MERGE` that sets `PlacedAt` sets all three together (§6.6). Splitting that statement would break these members without touching the predicate that appears to guarantee them |
| `ConcurrencyMode` | MassTransit enum selecting the saga repository's locking strategy (§9.6) |
| `NoInstanceForCancellation` | §9.6's `OnMissingInstance` branch for `OrderCancelled`: returns for the two arrivals this service can account for and throws MassTransit's own `SagaException` for every other, so the error-queue entry reads identically to the `PaymentAuthorised` one the machine already faults and `docs/runbooks/error-queue.md` needs one procedure rather than two. A **method** rather than a configurator call because `OnMissingInstance` takes one configurator and the decision needs the message. Listed despite the exemption above for members of defined types — that exemption rests on a member being visible at its declaration, and §9.6 names this one in a sample and shows no body |
| `GetConfiguredOptions` | Test helper returning the resilience options under assertion (§9.7) |
| `BuildServices`, `BuildProvider` | Test helpers running **both** real registration helpers — `AddOrderingApplication` and `AddOrderingInfrastructure` (§6.2, §6.3, §13.6). `BuildServices` returns the populated `IServiceCollection`; `BuildProvider` is `BuildServices().BuildServiceProvider()`. Two helpers because registrations can only be enumerated before the build |
| `OutboxBacklogHealthCheck` | Reports outbox depth as an `observe`-tagged check (§13.5) |
| `PlaceOrderCommand`, `PlaceOrderItem`, `PlaceOrderValidator`, `PlaceOrderHandler` | The worked command slice (§6.4). Carries **no** `CustomerId` — the handler takes the subject from `ICurrentUser` (§11.4's subject rule) |
| `ConfirmOrderCommand`, `MarkOrderShippedCommand`, `FlagOrderForReviewCommand`, and `ConfirmOrderHandler` / `MarkOrderShippedHandler` | The three saga-only command slices (§9.6) — the commands, and the two handlers this document does not print. `FlagOrderForReviewHandler` is printed in §9.6 and is therefore in D.4, not here. None carries a `CommandOrigin`, unlike `CancelOrderCommand`: that one has two ways in and needs the ownership check of §11.4 to tell them apart, while these have a single ingress and **no HTTP route** maps them — they are bound on `ordering-commands` like the fourth. A discriminator with one value is the kind of thing a later reader completes by adding the second ingress it implies. The first two carry a **domain** type (`PaymentReference`, `TrackingNumber`) parsed in the mapper, the third a string, because a review reason has no domain type behind it |
| `ConfirmStockCommand`, `ConfirmStockHandler` | The transition §5.4 documented with no caller until PR-21. An **internal** command with no wire contract: §3.2's Accepts column is closed at four, and what was missing was a consumer rather than a fifth thing a peer may send. Dispatched by `StockReservedHandler` |
| `AddressDto`, its `ToDomain()` | The shipping address as `PlaceOrderCommand` carries it, and the map to the `Address` value object (§6.4). Built by `AddressBuilder.ValidDto()` in §12.4. An application DTO, unrelated to `ShippingAddressV1` below — a wire contract and a command payload version on different schedules (§4.3) |
| `GetOrderSummariesQuery`, `GetOrderSummariesHandler`, `OrderSummaryDto`, `SummaryProduct` | The worked query slice — shown at level 1 (§6.5) and rewritten in place at level 2 (§6.6). Carries `Cursor` and `Limit` only: the `CustomerId` in the `WHERE` clause is bound from `ICurrentUser` at both levels (§11.4's subject rule). The projection feeding it is `OrderSummaryProjection`, defined in D.4. `SummaryProduct` is composed on read: the `Products` column holds a bare `Guid` array and the names come from `ordering.Products` in a second, page-bounded statement. It used to be **also the element the projection serialised**, with its three JSON names pinned by `[JsonPropertyName]` because the `JSON_MODIFY` paths in the patch handler spelled them a third time; [ADR-027](appendix-a-adrs.md#adr-027--the-order-summary-stores-product-ids-and-resolves-the-name-locally) removed the third spelling and the wire names with it |
| `GetProductDetailQuery`, `GetProductDetailHandler`, `ProductDetailDto`, `ProductSql` | Catalog's cached read slice — the `HybridCache` worked example (§8.2), and the one sample here belonging to a service other than Ordering |
| `ShippingAddressV1` | The address `V1.OrderConfirmed` carries (§9.1) — primitives only, and versioned with the contract that owns it, exactly as `PlacedLine` and `ConfirmedLine` are. **`Line2` arrived with the first producer**, which is PR-21's mapper: until something populated the type the omission was invisible, and the moment one did it was a flat number dropped between an order and the parcel. A contract with no consumers is the only cheap moment a contract ever has |
| `ProductPublished` | Catalog contract, envelope (§9.1) plus `ProductId`, `Name`, `ThumbnailUrl`, `Currency`, `Amount`. The last two are carried so a projection has a price the moment the product exists, without waiting for a `PriceChanged` that may never come; `Name` and `ThumbnailUrl` so it has something to show beside the price, on the same terms. **All four are spoken for**, which they were not before [ADR-027](appendix-a-adrs.md#adr-027--the-order-summary-stores-product-ids-and-resolves-the-name-locally): `ProductPriceProjection` keeps the price and is built; `OrderSummaryProjection` keeps the name and is not, so `Name` and `ThumbnailUrl` still travel on every message with nothing in `src/` reading them. Both run off the one `ordering-catalog-events` endpoint |
| `ProductDiscontinued` | Catalog contract, envelope plus `ProductId`. No reason code: §6.6 flips `IsAvailable` either way |
| `StockReserved`, `StockReleased` | Two of the saga's integration events ([§3.2](03-bounded-contexts.md), §9.6, `Common.Contracts`), each carrying the §9.1 envelope plus `OrderId` and nothing else — the step's whole outcome is that it happened. **`StockReleased` reports a postcondition rather than a state change** ([ADR-024](appendix-a-adrs.md#adr-024--a-release-answers-for-the-order-not-for-the-reservation)), which is why a payload saying what was released would be the wrong shape: there may have been nothing. `PriceChanged` is not here at all; §9.1 declares it |
| `StockReservationFailed` | Envelope, `OrderId`, and `UnavailableProductIds` — the fact the step decided. It is on the contract rather than left to a support query because the saga finalises on it, and `SetCompletedWhenFinalized` has deleted the instance by the time anyone asks which lines failed. Ids, not a message: a sentence is something every consumer has to parse |
| `StockLevelChanged` | Inventory's fourth event and Catalog's only subscription (§3.2). Envelope, `ProductId`, `QuantityAvailable` — a **level**, not a delta, which is what makes a consumer idempotent by construction rather than by care (§6.6) |
| `PaymentAuthorised`, `PaymentRefunded` | Envelope, `OrderId`, `Reference`, `Amount` and `Currency`. The reference is an opaque provider token and stays a string everywhere; the currency travels with the amount for §9.6's reason |
| `PaymentDeclined` | Envelope, `OrderId`, `Reason` — the **provider's** reason, deliberately not a closed vocabulary. `CancelReasons` is this platform's and is enumerated; a PSP's set is not ours to pin, so this is carried for a human and never branched on or used as a metric dimension (§9.8) |
| `ShipmentDispatched`, `ShipmentDelivered` | Envelope, `OrderId`, `TrackingNumber`. Despatch is what the saga finalises on; delivery has only Notifications for a consumer, because it is not something a saga can coordinate or compensate |
| `OrderConfirmed` | Envelope, `OrderId`, `CustomerId`, `TotalAmount`, `Currency`, `Lines`, `ShippingAddress` — §9.1's fat-enough event, sized for Shipping, which cannot despatch without the address. **Ordering consumes it too, and reads only `OrderId`** (§3.2, §9.6): it is the acknowledgement §9.6's `AwaitingConfirmation` waits for, and the only evidence that the `ConfirmOrder` the saga sent actually committed. A consumer that needs one field of a contract sized for another is not a reason to slim it — the address is still what Shipping was asked for |
| `OrderCancelled` | Envelope, `OrderId`, `CustomerId`, `Reason` — a `CancelReasons` code — and `Origin`, a `CancelOrigins` one. When the saga caused the cancellation it is the code the saga sent on `CancelOrder`, so the fact is reported with the reason that produced it; §11.4's endpoint sends whatever the caller asked for, which is **any** of the five codes `CancellationReasons` parses and not `customer_request` alone. **So `Reason` does not identify the origin**, which is still true and is what `Origin` is for: this entry first said the reason identified the origin, then that no field did, and [#123](https://github.com/alexander-shamray/dotnet-ddd-blueprint/issues/123) closed the gap the second correction named — by adding a field, which is the route the chapter offered, rather than by rereading the ones already there. `Origin` is **optional**, and absent means "published before the field existed" rather than "unknown" — §9.2 makes a new optional field additive, so this is not a V2, and the tolerance is **permanent for this contract version** rather than a phase with a tightening owed — a payload predating the field can still arrive from the error queue or a replay, so making `Origin` required would break every retained one and is a V2 by §9.2. **The saga consumes it as well as causing it** (§3.2, §9.6): the endpoint is the only way a customer's cancellation reaches the workflow, and a copy correlating to no instance is discarded on `workflow` — the service's own echo — and on the absent field, and **faults on everything else**. An allow-list rather than a deny, because the other shape passes every spelling nobody thought of; and faulting is the point, since a customer's cancellation overtaking its own `OrderPlaced` is the arrival that has to spend §9.8's retry envelope and then reach the error queue §13.6 pages on |

## D.6 Framework types

Not listed individually: ASP.NET Core, EF Core, MassTransit,
StackExchange.Redis, Dapper, Polly, OpenTelemetry, YARP, xUnit, Shouldly,
NSubstitute, Testcontainers, Respawn, Scrutor, FluentValidation, NetArchTest
and Aspire types are assumed — including the ones the samples name outright,
such as `TestResult` (§4.2), `IProxyStateLookup` ([§12.4](12-test-strategy.md))
and `IResourceBuilder<ProjectResource>` (§14.2). Licences in
[Appendix B](appendix-b-licences.md).

**The base class library and `Microsoft.Extensions.*` are assumed on the same
terms**, and the family is named here rather than left implicit because a rule
that lists fifteen library names reads as exhaustive. `IServiceCollection`,
`IHostedService`, `IOptions<T>`, `ILogger<T>`, `Meter` and `IMeterFactory`
(§13.3), and `MemoryCache` (§13.6) are all in it. Naming the family was owed
before PR-24 and became visible with it: `Microsoft.Extensions.Caching.Memory`
is now a direct pin with an Appendix B row, which is the half this appendix
does not cover.

One of those names collides: xunit.v3 declares its own `Xunit.TestResult`, so
a test file holding §4.2's gates aliases the one it means —
`using TestResult = NetArchTest.Rules.TestResult;` — the `Types` shadowing
note in D.5, met from the other direction.

---

[← Appendix C](appendix-c-delivery-plan.md) · [Index](README.md)
